# Backup_Zero/robo (pointer)

This workspace folder is just a **pointer**. The canonical archive location is:

- `/Volumes/Backup_Zero/robo/`

Use that for storing no-longer-valid knowledge.

Guidelines:
- Move/duplicate outdated notes, configs, or instructions here instead of deleting.
- Keep subfolders organized by topic/date (e.g., `2026-02/`, `whatsapp/`, `plugins/`).
- Prefer leaving a short pointer in the “active” document to where it was archived.
